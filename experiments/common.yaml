dataset:
    sequence_len: 256
    batch_size: 64
    alphabet_size: 8
    mem_tokens: 8
    selective: False

trainer:
    steps: 400000
    optimizer_class: torch.optim.Adam
    optimizer_params:
        lr: 0.0001
    device: "cuda"

metric_tracker:
    logging_steps: 100
    enable_progress_bar: False
    patience: 40
    repo: "/mnt/disk1/folivo/ssm"